# ScholarType / JATS2PDF 技术栈

本文档描述当前实际运行的架构。依赖版本以 `requirements*.txt`、各级 `package.json` 和 lockfile 为准，避免把某台开发机的补丁版本写死在文档中。

## 技术栈总览

| 层级 | 技术 | 用途 |
|---|---|---|
| 后端语言 | Python 3.10+ | 解析、存储、API 与 PDF 流水线 |
| Web API | FastAPI、Uvicorn | 页面托管、上传、文章查询和导出接口 |
| 数据存储 | SQLite、pickle | 文章索引、结构化 Article 对象与运行数据 |
| XML 解析 | lxml | JATS XML、XPath、MathML 与交叉引用解析 |
| 模板 | Jinja2 | CLI HTML、后端预览/PDF 和无前端构建时的回退页面 |
| PDF | WeasyPrint | HTML/CSS Paged Media 转 PDF |
| PDF 回退 | Chrome/Edge/Brave/Chromium headless | WeasyPrint 不可用时打印 PDF |
| 图片处理 | Pillow | EXIF 方向修正、缩放、格式标准化与内嵌 |
| 公式 | Node.js、mathjax-node | MathML/LaTeX 预渲染为 SVG |
| 前端 | React 18、TSX | 上传门户、文章库和编辑工作台 |
| 构建与样式 | Vite 6、Tailwind CSS 4 | 开发服务器、生产构建和门户样式 |
| 图标 | Lucide React | 两套 React 应用的图标组件 |
| 测试 | pytest | 单元测试和后端集成测试 |

前端 TSX 由 Vite/esbuild 转译；当前仓库没有独立的 `tsc` 类型检查流程。

## 运行架构

```text
                         ┌──────────────────────────────┐
                         │ FastAPI :8000                │
                         │                              │
浏览器 ─────────────────►│ /           React 门户       │
                         │ /studio/    React 工作台     │
                         │ /api/*      JSON/HTML/PDF API│
                         └──────────────┬───────────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    ▼                   ▼                   ▼
             JATSParser/lxml      SQLite + pickle     Jinja2 模板
                    │                                       │
                    ▼                                       ▼
             Article 数据模型                         WeasyPrint PDF
                                                            │
                                              Chrome headless 回退
```

生产构建后，FastAPI 从以下目录托管资源：

- `web/upload/dist`：门户与文章库，基路径 `/portal-assets/`
- `web/article/dist`：文章编辑工作台，基路径 `/studio-assets/`

如果 `dist` 不存在，后端仍可使用 `src/templates/` 和 `assets/` 提供基础回退页面。因此这些 Jinja 模板与旧静态资源仍属于有效运行代码，不能作为废弃文件删除。

## 后端分层

### JATS 解析

`src/parser/jats_parser.py` 将 XML 转换为结构化 `Article`：

- 标题、期刊、DOI、年份和语言
- 作者、机构、邮箱和作者机构关联
- 中英文摘要与关键词
- 多级章节、段落 runs 和行内元素
- 图、表、块级/行内公式
- 图表自动编号、锚点和 `<xref>` 交叉引用
- Elsevier 与 GB/T 7714 所需的参考文献字段

### 存储与资源

`src/store.py` 使用 SQLite 管理文章索引，并保存结构化对象。上传的 ZIP 资源包必须包含一个 XML，可同时携带图片；图片按文章 ID 隔离保存，避免不同文章的同名文件互相覆盖。

`data/` 包含真实运行数据：

- `articles.db`：文章索引
- `articles/`：序列化文章对象
- `article_assets/`：上传资源包图片
- `pmc_assets/`：PMC 图片缓存

### HTML 与 PDF

后端门户预览和 PDF 都通过 `src/templates/article_preview.html` 生成同一份自包含 HTML。`src/server.py` 在导出前把图片转换为 data URI，并根据尺寸进行压缩和缩放；`PDFRenderer(css_path="")` 不再叠加旧 CLI 样式，避免预览与 PDF 出现卡片、跨栏等冲突。

CLI 仍使用 `src/templates/article.html` 和 `src/templates/styles.css`，这是独立但受支持的输出链路。

### 双栏排版

当前 Web 模板采用期刊式 A4 排版：

- 标题、作者、机构、摘要和关键词通栏
- 正文使用 CSS multicol 双栏
- 图、表和公式默认保持在所在栏内
- 页眉、横线和页码由 CSS Paged Media 生成
- 参考文献使用编号与悬挂缩进
- 自动断词关闭，避免 WeasyPrint 生成带空格的异常英文断词

### PDF 引擎

首选 WeasyPrint，因为它支持 `@page`、页眉页脚、页码、分页控制和多栏布局。导入 WeasyPrint 失败时，`pdf_renderer.py` 会依次查找常见 Chromium 浏览器并使用 headless 打印。

Chrome 回退能保证基本输出，但 running header 等 Paged Media 细节可能与 WeasyPrint 不完全一致。

### 公式

WeasyPrint 不执行 JavaScript，也不能直接高保真呈现 MathML。`FormulaRenderer` 调用 `mathjax-node` 输出 SVG，再嵌入 HTML/PDF。

默认目录是 `~/mjnode`，也可通过 `JATS2PDF_MJDIR` 指定：

```bash
npm install --prefix ~/mjnode mathjax-node@2.1.1
```

未安装时使用 passthrough 模式；浏览器预览模板还包含客户端 MathJax 回退，但后端 PDF 中公式质量取决于服务端 SVG 预渲染是否可用。

## 前端分层

### 门户 `web/upload`

- JATS XML/ZIP 上传
- 文章库搜索、分页和刷新
- 文章预览参数控制
- 后端 HTML/PDF 下载
- 统一导航到文章工作台

门户大量使用 Tailwind 工具类，因此保留 Tailwind CSS 4 和 `@tailwindcss/vite`。

### 工作台 `web/article`

- 从 `/api/articles/{id}/editor` 加载真实结构化论文
- 编辑基础信息、摘要、章节、图表和参考文献
- 中英/双语与单双栏预览
- 浏览器打印 PDF
- 将当前预览 DOM 导出为 Word `.doc`

当前没有后端 PUT/PATCH 保存接口，编辑结果不会写回 SQLite。这是现阶段明确的功能边界。

## 依赖管理

### Python

- `requirements.txt`：生产运行依赖
- `requirements-dev.txt`：在运行依赖基础上增加 pytest 和 Black

已移除没有源码引用的 `bibtexparser`。PyMuPDF、pypdf 只用于人工 PDF 视觉验收，不属于运行依赖。

### Node

根 `package.json` 只负责协调两个 Vite 子项目。每个子项目仅保留实际入口可达的依赖：

- `react`
- `react-dom`
- `lucide-react`
- `vite`
- `@vitejs/plugin-react`
- `tailwindcss`
- `@tailwindcss/vite`

未引用的 MUI、Radix、shadcn、Recharts、DnD、KaTeX 等脚手架依赖已经删除。

## 验证流程

```bash
npm run web:install
npm run web:build
.venv/bin/python -m pytest tests/ -q
.venv/bin/python -m src.main samples/sample1.xml \
  --two-column \
  --no-render-formulas \
  -o /tmp/qikanproblem-smoke.pdf
```

服务启动后再验证：

```bash
curl -fsS http://127.0.0.1:8000/api/health
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/studio/
```

测试覆盖解析、模板、公式、存储、上传大小限制、安全 ZIP 解包、图片资源隔离、PMC 图片回退、PDF 图片嵌入，以及预览/PDF 文档一致性。
