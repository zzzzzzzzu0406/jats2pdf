# JATS2PDF 技术栈文档

> 期刊大赛 · 选题2：基于 JATS XML 结构数据的 PDF 自动排版与生成
>
> 本文档完整描述 jats2pdf 项目的技术选型、架构、依赖、运行环境、关键设计与已知限制。

---

## 一、技术栈总览

| 层级 | 技术 | 版本 | 作用 |
|------|------|------|------|
| 语言 | Python | 3.12（conda env）/ 3.14（.venv 开发） | 主语言 |
| XML 解析 | lxml | 6.1.1 | JATS XML 解析 + XPath 查询 |
| 模板引擎 | Jinja2 | 3.1.6 | HTML 模板渲染（含宏：自动编号/交叉引用/参考文献格式） |
| PDF 生成（首选） | WeasyPrint | 69.0 | HTML+CSS Paged Media → PDF |
| PDF 生成（回退） | Chrome headless | 150.0 | 无 GTK 时打印 PDF |
| 公式渲染 | mathjax-node | 2.1.1 | MathML/LaTeX → SVG 矢量预渲染 |
| 公式运行时 | Node.js | 24.16 | 驱动 mathjax-node |
| 系统库（GTK） | pango / cairo / glib / harfbuzz / fontconfig / freetype | 1.56 / 1.18 / 2.88 / 14.2 / 2.18 / 2.14 | WeasyPrint 文本布局与字体（conda-forge 自带） |
| 测试 | pytest | 9.1.1 | 单元 + 集成测试（15 项） |
| 验证工具 | PyMuPDF / pypdf | 1.28 / 6.14 | PDF 渲染检查 / 文本提取（开发用） |
| 前端 | 原生 HTML + CSS + JS | — | 多页面平台（首页/浏览/上传/详情/关于） |

---

## 二、系统架构与流水线

```
┌────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────┐
│ JATS XML   │ → │ JATSParser   │ → │ FormulaRenderer│ → │ HTMLRenderer │ → │ PDF  │
│ (1.0/1.1)  │   │ (lxml)       │   │ (MathML→SVG) │   │ (Jinja2)     │   │      │
└────────────┘   └──────────────┘   └──────────────┘   └──────────────┘   └──────┘
                        │                                      │            │
                        └─→ Article 数据模型 ──────────────────┘            │
                        └─→ xref_map (rid→编号/锚点/标签)                    │
                                                                  WeasyPrint (首选)
                                                                  Chrome headless (回退)
```

### 流水线四步（`src/main.py`）

1. **解析 JATS XML**：`JATSParser` 用 lxml 解析，输出结构化 `Article` 对象（标题/作者/机构/双语摘要/章节树/图表公式/参考文献 + `xref_map`）。
2. **公式预渲染**（默认开启）：`FormulaRenderer` 经 mathjax-node 把 MathML/LaTeX → SVG，写回 `Formula.mathml`。
3. **渲染 HTML**：`HTMLRenderer` 用 Jinja2 模板把 `Article` → 自包含 HTML（内联 platform.css/js）。
4. **生成 PDF**：`PDFRenderer` 优先 WeasyPrint（应用 styles.css 的 CSS Paged Media），不可用则回退 Chrome headless。

---

## 三、分层详解

### 1. XML 解析层 — `src/parser/jats_parser.py`（lxml 6.1.1）

- **为什么 lxml**：比标准库 `xml.etree` 快 10 倍+，支持 XPath、命名空间、`itertext()`，JATS 命名空间（jats/mml/xlink/ali）处理稳健。
- **数据模型**（dataclass）：`Article` / `Author` / `Affiliation` / `Figure` / `Table` / `Formula` / `Reference` / `Run` / `Paragraph` / `Section`。
- **关键能力**：
  - 多级章节嵌套（`_get_section_level` 按 `<sec>` 嵌套深度推算 1–6 级）
  - 段落内联结构：`Paragraph.runs` 为 `TextRun`/`XrefRun`/`FormulaRun` 序列，保留 `<xref>` 与 `<inline-formula>` 在正文中的位置（不再被 `itertext()` 压平）
  - 中英双语：`abstract` / `abstract_en`（按 `<abstract xml:lang>` 与 `<trans-abstract>` 区分）；`keywords` / `keywords_en`（按 `<kwd-group xml:lang>` 分组）
  - 机构：解析 `<aff>` + 作者 `<xref ref-type="aff">` 关联，生成机构序号上标
  - 参考文献：解析 `<person-group>/<name>`（JATS 标准，非 `person-name`）、起止页（fpage-lpage）、`publication-type`、出版社
  - 自动编号 + `xref_map`：DFS 按文档顺序对图/表/块级公式编号（`图N`/`表N`/`(N)`），登记参考文献（`[N]`），构建 `rid → {type, number, anchor, label}` 映射，供模板渲染可点击交叉引用
  - 语言检测：`xml:lang` 或标题含汉字 → `article.lang`（zh/en），决定标签语言

### 2. 模板渲染层 — `src/renderer/html_renderer.py` + `src/templates/`（Jinja2 3.1.6）

- **多页面平台**：`render_index/browse/upload/about/article`，共享 `base.html`（内联 platform.css + platform.js）。
- **article.html 关键设计**：
  - **图表/公式扁平化**：`render_section` 宏把图/表/公式从 `<section>` 内提升为 `.layout-main` 直接子元素——WeasyPrint 的 `column-span:all` 仅对 multicol 容器直接子元素生效，扁平化后双栏跨栏才工作。
  - **段落 runs 渲染**：`xref` → `<a href="#anchor" class="xref">{label}</a>`（label 经 `xref_map` 解析为 `图N`/`[N]` 等，多 rid 合并 `[2], [3]`）；`formula` → 行内 SVG；`text` → 原文。
  - **自动编号**：图/表标签用 `{{ fig.label }}`（解析器已按语言生成 `图1`/`Figure 1`）。
  - **参考文献宏**：`ref_elsevier` / `ref_gbt` 两种格式串，由 `ref_style`（`--ref-style`）切换。
  - **双语/机构**：英文摘要块、英文关键词、机构上标 + 机构列表。
  - **`@media print` 剥离 web 外壳**：打印时隐藏 navbar/侧边目录/面包屑/页脚，只留论文正文。

### 3. 公式预渲染层 — `src/renderer/formula_renderer.py`（mathjax-node 2.1.1）

- **问题**：WeasyPrint 不执行 JavaScript、也不渲染 MathML；直接嵌 `<mml:math>` 会退化成裸文本堆叠（`h i σ W ...`），公式结构丢失。
- **方案**：用 mathjax-node 把 MathML/LaTeX 预渲染为矢量 SVG，写回 `Formula.mathml`，模板 `{{ fm.mathml | safe }}` 输出 `<svg>`，WeasyPrint 完美渲染矢量。
- **实现**：自举生成 `~/mjnode/mathml2svg.js`（调 `mathjax-node.typeset`），`_run_helper` 调 `node` 转 SVG；`_detect_method` 实测最小 MathML 验证可用性（检查 returncode，修复了旧版误判 bug）；`process_article_formulas` 处理块级公式 + 段落内行内公式。
- **中文路径坑**：mathjax-node 对含非 ASCII 的安装路径有 bug（内部 URL 编码路径导致找不到 MathJax.js），故须装在无中文路径 `~/mjnode`。
- **降级**：未装 mathjax-node 时 `passthrough`（原样 MathML，PDF 中公式退化为文本）。

### 4. PDF 生成层 — `src/renderer/pdf_renderer.py`（双引擎自动切换）

- **首选 WeasyPrint 69.0**：支持 CSS Paged Media——`@page`（页眉页脚、`counter(page)` 页码、`@page:first` 首页无页眉）、`column-count`/`column-span`（双栏）、`break-inside:avoid`（图表不跨页）、`string-set`（running header）。这是赛题需求 6/7 的原生实现基础。
- **回退 Chrome headless**：WeasyPrint 导入失败（缺 GTK）时自动用系统 Chrome `--headless=new --print-to-pdf`。零额外安装，但不支持 `@page` running header（页码仍可，期刊名 running header 丢失）。
- **静默**：WeasyPrint 导入失败时向 stdout 打印的安装指引被 `redirect_stdout/stderr` 捕获丢弃，避免误导为「生成失败」。
- **WeasyPrint 的 GTK 依赖**（macOS）：需 pango/cairo/glib/harfbuzz/fontconfig/freetype。本机无 Homebrew（且 `raw.githubusercontent.com` 超时装不了 brew），改用 **conda-forge 自带 GTK 的 weasyprint**——无 sudo、无 brew（anaconda 主机可达）。

### 5. 样式层 — `src/templates/styles.css` + `assets/css/platform.css`

- **styles.css（打印样式表）**：PDFRenderer 经 `CSS(filename=...)` 注入。含：
  - `@page`：A4、页边距 2.5/2/2.5/2cm、`@top-left` 期刊名 running header（`string(journalname)`）、`@top-right` 页码（`counter(page)`）、`@page:first` 首页无页眉
  - `@media print`：按《设计规范》pt 字号（标题 16/12/12-11-10pt、正文 10pt、行距 1.6、摘要 2em、表与参考文献 8.5pt）
  - `body.two-column`：`.layout-main` `column-count:2` + 摘要/图表/公式/参考文献 `column-span:all`
  - `break-inside:avoid`（图表/公式不跨页）、SVG 尺寸约束、xref/affiliation/`:target` 样式
- **platform.css（web 平台样式 v1.0）**：被 `html_renderer` 内联进 HTML。设计系统（CSS 变量）、深蓝学术风、卡片/hero/导航/响应式。字体栈含 PingFang SC/Songti SC（macOS 回退）。
- **v2.0/v3.0 类名对齐**：styles.css 原用 `.article-body p`（v2.0），v3.0 模板改用 `.article-content`，已统一修正，避免死规则。

### 6. 测试层 — `tests/`（pytest 9.1.1，15 项）

- `test_parser.py`：解析（双语/机构/编号/xref/ref 作者页码/inline-formula）
- `test_renderer.py`：模板渲染（xref 链接/自动编号/双语/机构/Elsevier·GB-T 7714）
- `test_formula_renderer.py`：公式预渲染（passthrough/检测/SVG 集成/行内公式）

---

## 四、运行环境

### Python 环境（双 env）

| 环境 | Python | 用途 | 关键包 |
|------|--------|------|--------|
| conda env `jats2pdf` | 3.12.13 | **WeasyPrint 原生跑 PDF**（含 GTK） | weasyprint 69.0、pango、lxml、jinja2、pytest |
| `.venv` | 3.14.5 | 开发/Chrome 回退/验证 | 同上依赖（weasyprint 惰性，走 Chrome）+ pymupdf、pypdf |

### 系统依赖

- **Node.js 24.16** + **mathjax-node 2.1.1**（装于 `~/mjnode`，无中文路径）
- **Google Chrome 150**（回退引擎）
- **中文字体**：macOS 自带 STHeiti；字体栈含 PingFang SC / Songti SC / SimHei / SimSun / Noto CJK

### 环境搭建命令

```bash
# 1. WeasyPrint 原生 env（conda-forge 自带 GTK，无 brew/sudo）
conda create -y -n jats2pdf --override-channels -c conda-forge python=3.12 weasyprint pango lxml jinja2 pytest
conda activate jats2pdf

# 2. 公式预渲染（mathjax-node，无中文路径）
mkdir -p ~/mjnode && cd ~/mjnode && npm install mathjax-node

# 3. 运行
python -m src.main samples/sample1.xml -o samples/output/sample1.pdf
```

---

## 五、依赖清单（requirements.txt）

| 包 | 版本约束 | 层级 |
|----|----------|------|
| weasyprint | >=60.0 | PDF 生成（首选） |
| jinja2 | >=3.1.0 | 模板 |
| lxml | >=5.0.0 | XML 解析 |
| bibtexparser | >=1.4.0 | BibTeX 解析（可选，当前未用） |
| pytest | >=8.0.0 | 测试 |
| black | >=24.0.0 | 格式化（开发） |

> Node 依赖 mathjax-node 不在 requirements.txt（非 Python 包），单独 `npm install`。

---

## 六、关键技术决策与权衡

### 1. 为什么选 WeasyPrint 而非 LaTeX/iText/PDFBox

- **Python 原生**：与解析/模板层同语言，无缝集成。
- **CSS Paged Media**：用 CSS 控制 `@page`/页眉页码/分栏/分页，比 LaTeX 编程式排版更灵活，符合「HTML+CSS→PDF」的现代 web 排版范式。
- **中文友好**：配合 pango/harfbuzz，中文断行/字体处理好。
- 权衡：WeasyPrint 不跑 JS（故公式需预渲染 SVG）、不渲染 MathML、对部分 CSS（box-shadow/CSS 变量场景）支持有限——这些是已知边界，已用预渲染/`@media print` 规避。

### 2. 双引擎自动回退

WeasyPrint 依赖系统 GTK，环境受限（如本机无 brew）。回退 Chrome headless 保证「任何装了 Chrome 的机器都能出 PDF」，代价是丢失 `@page` running header。`pdf_renderer.py` 自动检测、无感切换。

### 3. 公式 SVG 预渲染（而非客户端 MathJax）

WeasyPrint 不跑 JS，故选「预渲染 SVG 嵌入 HTML」而非「CDN MathJax 客户端渲染」。SVG 是矢量，PDF 中无限缩放不失真，满足「高保真」。代价：依赖 Node + mathjax-node，路径需无中文。

### 4. 图表/公式扁平化以支持双栏跨栏

WeasyPrint 的 `column-span:all` 仅对 multicol 容器直接子元素生效。模板把图/表/公式从 `<section>` 提升为 `.layout-main` 直接子元素，单栏无害、双栏正确跨栏。这是赛题需求 6 的关键实现细节。

### 5. 自动编号 + xref_map（而非 CSS counter）

参考文献用 CSS counter 自增 `[N]`，但图表/公式的交叉引用需要「正文 xref 显示对应编号并链接到锚点」。用解析层 `xref_map`（rid→编号/锚点/标签）统一解决，支持多 rid 合并（`[2], [3]`）与未登记 rid 回退原文。

---

## 七、已知限制

1. **WeasyPrint `column-span:all` 仅对直接子元素生效** → 模板扁平化图表/公式为 `.layout-main` 直接子元素。
2. **WeasyPrint 不渲染 MathML** → 须 mathjax-node 预渲染 SVG；不跑 JS → 不用 CDN MathJax。
3. **WeasyPrint 部分 CSS 不支持**：`box-shadow`、CSS 变量部分场景、`@media (max-width)` 无媒体类型语法（仅影响 web 外观，不影响 PDF 排版）。
4. **mathjax-node 中文路径 bug** → 须装于 `~/mjnode`（无中文）。
5. **Chrome 回退路径**：不支持 `@page` running header（期刊名页眉丢失，页码保留）。
6. **PMC 等外部论文的图片**：JATS 仅引用图片文件名（如 `fig5.jpg`），需同目录存在；未下载时只显题注。

---

## 八、赛题要求（选题2）实现对照

| # | 要求 | 实现方式 | 技术 |
|---|------|----------|------|
| 1 | 标题/作者/摘要/关键词 | 解析+模板；中英双语；机构上标+列表 | lxml + Jinja2 |
| 2 | 正文+章节流式排版 | 多级章节；首行缩进2em、行距1.6、pt 字号 | CSS `@media print` |
| 3 | 图表自动编号+交叉引用 | `xref_map` + 模板 `<a href>` 可点击链接 | 解析层编号 + Jinja2 宏 |
| 4 | MathML 高保真渲染 | mathjax-node 预渲染 SVG（块级+行内） | mathjax-node |
| 5 | 参考文献格式化 | Elsevier / GB-T 7714 宏（`--ref-style`）、悬挂缩进2em | Jinja2 宏 + CSS |
| 6 | 单/双栏切换 | `--two-column`、`column-count` + 扁平化 `column-span:all` | CSS Paged Media |
| 7 | 页眉页脚/页码 | `@page` running header + `counter(page)` + 首页无页眉 | CSS `@page` |

---

## 九、项目结构

```
期刊大赛/
├── src/
│   ├── parser/jats_parser.py        # JATS 解析 + 数据模型 + xref_map + 自动编号
│   ├── renderer/
│   │   ├── html_renderer.py         # Jinja2 多页面渲染
│   │   ├── pdf_renderer.py          # WeasyPrint→PDF（自动回退 Chrome）
│   │   └── formula_renderer.py      # MathML/LaTeX→SVG（mathjax-node）
│   ├── templates/
│   │   ├── article.html             # 论文详情（xref/扁平化/双语/机构/Elsevier·GB-T）
│   │   ├── base.html / *.html       # 平台页面
│   │   └── styles.css               # 打印样式（@page/@media print/.two-column）
│   └── main.py                      # CLI 入口
├── assets/css/platform.css          # web 平台样式
├── assets/js/platform.js            # web 平台脚本
├── samples/sample1.xml              # 中文示例（双语/机构/fig/xref/inline-formula）
├── samples/real/                    # 真实 PMC 论文（验证用）
├── build_site.py                    # 静态站点构建
├── tests/                           # 15 项单元/集成测试
├── docs/                            # 设计规范、项目说明、技术栈
└── requirements.txt
```
