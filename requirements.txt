# ============================================
# 期刊大赛 - 选题2: JATS XML → PDF 智能排版
# ============================================

# 核心依赖
weasyprint>=60.0          # HTML → PDF 渲染引擎
jinja2>=3.1.0             # 模板引擎，HTML模板渲染
lxml>=5.0.0               # JATS XML 解析

# 公式处理（可选方案）
# 方案A: pymathml 解析 MathML → LaTeX
# 方案B: 在HTML中嵌入 MathJax/KaTeX，WeasyPrint渲染时处理

# 文献处理
python-docx>=1.1.0        # 如果后续需要处理Word引用
bibtexparser>=1.4.0       # 解析 BibTeX 参考文献

# 开发工具
pytest>=8.0.0             # 单元测试
black>=24.0.0             # 代码格式化
isort>=5.13.0             # import 排序

# 字体支持（中文排版）
# WeasyPrint 需要系统中文字体，建议安装 Noto Sans CJK
